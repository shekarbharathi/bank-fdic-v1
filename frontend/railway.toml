# Used when this Railway service has Root Directory set to `frontend/`.
# Paths are relative to this directory (the service root).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
