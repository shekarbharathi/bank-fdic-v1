# Procfile for Railway (alternative to railway.json)
# Railway can use this to determine how to run your services

# Backend service
web: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT

# Frontend service (if deploying separately)
# frontend: cd frontend && npm run start
